1.redis连接池优化 
  短期记忆每次调用都执行redis.REDIS,一个新的redis对象背后是一次完整的tcp三次握手，当前项目的一次
请求要调4次redis（追加，更新workmemory，trim，更新元数据），不仅是延迟问题，操作系统临时端口也可能耗尽
  现在创建一个公共的连接池，通过复用连接池的连接，来降低频繁建立连接的开销。并且配合socket_timeout
  （一次redis命令等多久，如果5s没返回，就抛出异常，防止无限等待）和health_check_interval
  （多久检测一次空闲连接，防止死连接常驻，白等一个超时）来做健康检测
2.线程池以及监控
  原来的代码流式输出每一次都创建一个新的线程池，用完又完全销毁，没有复用。现在所有请求共享_executor
  (10个线程)，为了避免共享资源的上限，要监控（当前线程使用数，请求等待数，容量上限，历史处理了多少请求，
  配合时间看QPS，了解吞吐量）
  创建线程要分配栈内存、要切换内核态度。共享线程不是为了请求更快，而是为了让系统在压力下面
  不崩盘，并且出问题可以观测到。

3. Python 有 GIL（全局解释器锁），同一时刻只有一个线程在跑 Python 代码。
  但 FastAPI 中用线程池仍然有效，提高并发。
  python里都有的对象，都靠引用计数管理内存，GIL保证计数操作不会被打断，所以python不需要对每个
  对象加锁，内存管理简单安全。