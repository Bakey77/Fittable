# 修复日志

## 2026-04-27: working_memory 写回链路并发覆盖修复

### 问题描述

`workflow.py` 在写回 `working_memory` 时存在快照过期导致的并发覆盖：

1. `line 242`: 在 workflow 开头读取 `working_memory` 快照
2. `line 283`: workflow 执行（含 LLM 调用，耗时数秒）
3. `line 320-325`: 节点未返回某字段时，用快照旧值回填
4. `line 327`: 调用 Lua 原子写入

**问题**: Lua 是原子的，但传入的参数来自几秒前的快照。T1 到 T4 之间另一个请求的 `working_memory` 修改会被覆盖。

### 竞态时序

```
T1  请求A: get_working_memory() → {waiting_info: A, pending_intent: X}
    进入 workflow（LLM 执行 3-5 秒）...

T2  请求B: update_working_memory(waiting_info=B, pending_intent=Y) → Lua 写入成功
    Redis: {waiting_info: B, pending_intent: Y}

T3  请求A: workflow 结束，结果中未返回 pending_intent
    → 回填快照旧值 pending_intent = X  ← 过期！

T4  请求A: update_working_memory(waiting_info=C, pending_intent=X)
    Redis: {waiting_info: C, pending_intent: X}  ← B的Y被覆盖！
```

### 修复方案

**将"保留旧值"的决策从 Python 快照下沉到 Lua 内部**，让 Lua 在写入时刻读取 Redis 当前值来决定。

每个字段引入三态操作语义：

| op 参数 | 含义 | 何时使用 |
|---------|------|----------|
| `KEEP` | 不修改，保留 Redis 当前值 | 节点未返回该字段 |
| `CLEAR` | 显式清空 | 节点返回 `None` |
| `SET` | 写入新值 | 节点返回非 `None` 值 |

### 修改文件

#### 1. `Agent/memory/session_memory.py`

**Lua 脚本重写** (`_LUA_UPDATE_WORKING_MEMORY`, line 55-99):

ARGV 布局变更：
```
旧: ARGV[1]=waiting_info_json, ARGV[2]=pending_intent, ARGV[3]=pending_entities_json, ARGV[4]=ttl
新: ARGV[1]=wi_op, ARGV[2]=wi_val, ARGV[3]=pi_op, ARGV[4]=pi_val, ARGV[5]=pe_op, ARGV[6]=pe_val, ARGV[7]=ttl
```

每个字段独立判断：
- `SET` → 解码 JSON 后写入
- `CLEAR` → 设为 nil
- `KEEP` → 不操作，保留 Redis GET 读到的当前值

**Python 函数签名变更** (`update_working_memory`, line 401-455):

```python
# 旧签名
def update_working_memory(session_id, waiting_info, pending_intent, pending_entities)

# 新签名（每个字段独立 op，默认 KEEP）
def update_working_memory(session_id,
                          waiting_info_op="KEEP", waiting_info=None,
                          pending_intent_op="KEEP", pending_intent=None,
                          pending_entities_op="KEEP", pending_entities=None)
```

fallback 路径同步更新，按 op 类型处理 `_fallback_store`。

#### 2. `Agent/workflow.py` (line 313-335)

**旧逻辑（已删除）**：
```python
next_waiting_info = result.get("waiting_info")
if next_waiting_info is None and "waiting_info" not in result:
    next_waiting_info = working_memory.get("waiting_info")  # 用快照旧值
# ... 整块覆盖写入
```

**新逻辑**：
```python
def _op(key):
    if key not in result:    return "KEEP", None   # 节点未返回 → Lua 保留当前值
    val = result[key]
    return ("CLEAR", None) if val is None else ("SET", val)  # 显式控制

wi_op, wi_val = _op("waiting_info")
pi_op, pi_val = _op("pending_intent")
pe_op, pe_val = _op("pending_entities")
update_working_memory(session_id, waiting_info_op=wi_op, waiting_info=wi_val, ...)
```

Python 侧不再引用快照旧值，所有"保留"逻辑由 Lua 在 Redis 内部完成。

### 并发安全性

| 场景 | 修复前 | 修复后 |
|------|--------|--------|
| 请求A节点未返回 waiting_info，请求B在A执行期间更新了 waiting_info | B的值被A的快照覆盖 | Lua 读到 B 的最新值，KEEP 保留，不覆盖 |
| 请求A节点返回 waiting_info=X，请求B在A执行期间更新了 waiting_info | B的值被X覆盖（符合预期：最新写入者胜出） | 同左 |

---

## 2026-04-27: metadata 并发覆盖修复

### 问题描述

`_increment_total_turns` 和 `mark_batch_processed` 使用 Python 侧的 GET → 修改 → SETEX 模式，与 Lua 原子操作并发时会覆盖数据。

### 修复方案

新增 `_LUA_UPDATE_METADATA` Lua 脚本 + `update_metadata()` 公共函数。

### 修改文件

- `Agent/memory/session_memory.py`: +Lua 脚本 `_LUA_UPDATE_METADATA`, +`update_metadata()` 函数
- `Agent/workflow.py`: `_increment_total_turns` → `update_metadata("increment_total_turns")`
- `Agent/memory/long_memory.py`: `mark_batch_processed` → `update_metadata("mark_batch_processed")`
- `Agent/memory/__init__.py`: 导出 `update_metadata`

---

## 当前短期记忆并发安全性总览

| 操作 | 并发保护方式 | 状态 |
|------|-------------|------|
| `append_turn` | Lua 原子 (GET+追加+SETEX) | 安全 |
| `update_working_memory` | Lua 原子 + 字段级 KEEP/CLEAR/SET | 安全 |
| `trim_and_summarize` | Lua 原子 (GET+裁剪+SETEX) | 安全 |
| `update_metadata` | Lua 原子 (GET+修改meta+SETEX) | 安全 |
| Redis 不可用时 fallback | `threading.Lock` 互斥 | 进程内安全 |
